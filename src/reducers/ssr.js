/* ------------------------------------------
 * SSR Reducer
 *------------------------------------------- */
const defaultState = {
  initialPath: '/'
}

export default function ssrReducer(state = defaultState, action) {
  switch (action.type) {
    default:
      return state
  }
}
