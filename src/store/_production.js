/* ------------------------------------------
 * Production store configuration
 * Used to add particular behaviors in prod env
 *------------------------------------------- */
import storeConfigurator from './_base'

const prodConfig = []

export default storeConfigurator(prodConfig)
